{
  "name": "@asyncflow/worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/main.ts",
    "start": "node dist/main.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@asyncflow/shared": "*",
    "@asyncflow/contracts": "*",
    "@asyncflow/database": "*",
    "@asyncflow/queue": "*",
    "@asyncflow/logger": "*",
    "@asyncflow/metrics": "*",
    "@asyncflow/config": "*",
    "@asyncflow/utils": "*",
    "bullmq": "^4.15.0",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.2"
  }
}
